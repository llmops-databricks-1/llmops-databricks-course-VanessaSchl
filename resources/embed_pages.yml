resources:
  jobs:
    embed_pages_job:
      name: embed-pages
      tags:
        project_name: "llmops-databricks-course-VanessaSchl"

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - ../dist/*.whl

      tasks:
        - task_key: embed_pages
          environment_key: default
          spark_python_task:
            python_file: ../notebooks/03_embed_pages.py
