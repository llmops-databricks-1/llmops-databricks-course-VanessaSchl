resources:
  jobs:
    crawl_pages_job:
      name: crawl-pages
      tags:
        project_name: "llmops-databricks"

      environments:
        - environment_key: default
          spec:
            environment_version: "5"
            dependencies:
              - ../dist/*.whl

      tasks:
        - task_key: crawl_pages
          environment_key: default
          spark_python_task:
            python_file: ../notebooks/02_crawl_pages.py
